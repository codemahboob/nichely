# Get this from https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Where submissions should land — your inbox
TO_EMAIL=you@yourdomain.com

# Optional. Must be a verified domain in Resend, otherwise leave this
# out and it will default to onboarding@resend.dev (test sender, only
# works reliably for a while — verify your own domain for production).
FROM_EMAIL=AdFex <hello@yourdomain.com>
